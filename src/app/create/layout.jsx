export const metadata = {
  title: "Create Post | FondPeace",
  robots: {
    index: false,
    follow: false,
  },
};

export default function CreateLayout({ children }) {
  return <>{children}</>;
}
